# Magyar Beszédfelismerő — Windows
# Default: CPU mode (works on any PC)
# For GPU support, use requirements-gpu.txt instead

faster-whisper>=1.1.0
ctranslate2>=4.5.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
